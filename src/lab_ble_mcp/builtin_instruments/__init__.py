"""Instrument definitions in the lab-executor ecosystem schema.

These declare the named commands ``list_commands`` and
``execute_named_command`` operate on. They sit on top of the byte-level field
maps in :mod:`lab_ble_mcp.profiles`, which the definitions do not duplicate.
"""
